----

More efficient use of indexes.

1) Should not create a secondary index for "all free".
   (Just get all the contents).
   
2) Use one BDB index (all bound index) for anything
   that is a prefix of the all bound index.
   
   